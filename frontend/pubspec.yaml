name: kolkata_tour_ai_frontend
publish_to: 'none'
description: Flutter frontend for Kolkata Tour AI
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  google_maps_flutter: ^2.9.0
  geolocator: ^10.1.0
  shared_preferences: ^2.3.2
  speech_to_text: ^6.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
